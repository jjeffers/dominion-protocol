---
trigger: always_on
---

When you need to create a temporary or log file for capturing output, use a "tmp" directory within the root of the project. 

The local tmp directory should be placed in the .gitignore, and no files within it should be committed to the repository.